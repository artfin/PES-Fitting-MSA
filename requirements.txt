# Core dependencies
scikit-learn>=1.0.2
matplotlib>=3.5.1
PyYAML>=6.0
tensorboard>=2.9.0
Pillow>=9.1.1
psutil
tqdm

# PyTorch - install with CUDA support:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# Or uncomment below for CPU-only:
# torch

# Optional: Google Drive integration
# pydrive>=1.3.1

# Optional: Experiment tracking
# wandb>=0.13.4

# extxyz - install from GitHub:
#   pip install git+https://github.com/libAtoms/extxyz.git
# Or if using ASE, extxyz reading is included:
#   pip install ase
